spring:
  data:
    mongodb:
      host: "localhost"
      port: 27017
      username: pnpmusicapp
      password: password
      database: pnpmusicapp
      authentication-database: admin
      auto-index-creation: true

app:
  file:
    location: /tmp/app/

logging:
  level:
    reactor:
      netty:
        http:
          client: DEBUG